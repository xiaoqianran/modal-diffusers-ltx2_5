HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MODEL_ID=Lightricks/LTX-2.5-Diffusers
MODEL_REVISION=69009ff070135c693ad1ad1ef2cc149c227963da
QUANTIZED_MODEL_DIR=LTX-2.5-Diffusers-bnb-4bit
OFFLOAD_MODE=model
OUTPUT_DIR=outputs
INPUT_DIR=inputs
MAX_UPLOAD_SIZE_MB=500
MAX_QUEUE_SIZE=4
# OpenAI-compatible external LLM for the optional prompt rewrite button.
# LLM_BASE_URL=http://127.0.0.1:1234/v1
# LLM_MODEL=your-model-name
# LLM_API_KEY=
# LLM_TIMEOUT_SECONDS=60
# Set to 1 only after the first successful download if the server has no network.
HF_HUB_OFFLINE=0
# --- Acceleration (2026-09) ---
# Transformer precision: nf4 (default) / fp8 (48GB-class) / bf16 (96GB-class) /
# nvfp4 (sm_120 Blackwell only, fastest; official FP4 distilled checkpoint)
# LTX25_TRANSFORMER_PRECISION=nvfp4
# CUDA Graph capture of the transformer forward (bit-identical, requires OFFLOAD_MODE=none)
# LTX25_CUDA_GRAPH=1
# LTX25_CUDA_GRAPH_MAX_CAPTURES=8
# Video encoder: nvenc (default) / x264. p4 preset recommended for realtime.
# LTX25_VIDEO_ENCODER=nvenc
# LTX25_NVENC_PRESET=p4
# Modal deployment / local router.
# The default path is now: local FastAPI router -> Modal SDK -> RTX PRO 6000 Worker.
# Set to 0 only if you do not want the local app to keep one GPU worker warm while open.
# LTX25_LOCAL_KEEP_GPU_WARM=1
# Warm worker idle window used while the local app is open.
# LTX25_MODAL_GPU_IDLE_SECONDS=600
# GPU memory snapshotting is experimental and OFF by default. For this ~45 GiB
# resident LTX worker, measured snapshot restore was slower than normal loading.
# LTX25_MODAL_GPU_SNAPSHOT=0
# Load Gemma and the NVFP4 transformer concurrently during cold start.
# LTX25_PARALLEL_COLD_LOAD=1
# Compatibility only: deploy the old remote CPU ASGI gateway when explicitly enabled.
# LTX25_MODAL_REMOTE_GATEWAY=0

# --- Media data plane ---
# Default keeps the current Modal Volume path and requires no extra services.
LTX25_MEDIA_BACKEND=volume

# Object-storage mode: browser uploads directly to S3-compatible storage and /outputs redirects
# to short-lived signed GET/HEAD URLs. Job records keep stable object keys only.
# Recommended self-hosted option: MinIO.
# LTX25_MEDIA_BACKEND=s3
# LTX25_S3_BUCKET=ltx25-media
# LTX25_S3_ENDPOINT_URL=https://minio.example.com
# LTX25_S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# Tigris uses the same generic S3 backend:
# LTX25_S3_ENDPOINT_URL=https://fly.storage.tigris.dev
# LTX25_S3_REGION=auto
# Cloudflare R2 also uses the same generic S3 backend:
# LTX25_S3_BUCKET=ltx25-media-r2
# LTX25_S3_ENDPOINT_URL=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# LTX25_S3_REGION=auto
# Presigned URLs: 1 second to 7 days. 1 hour is the default.
# LTX25_MEDIA_PRESIGN_SECONDS=3600
# Files >= this threshold use browser multipart upload.
# LTX25_MEDIA_MULTIPART_THRESHOLD_MB=96
# S3 multipart parts must be >=5 MiB. 16 MiB is the default.
# LTX25_MEDIA_PART_SIZE_MB=16

# Modal CloudBucketMount uses a Modal Secret containing:
# AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY
# modal secret create s3-media AWS_ACCESS_KEY_ID=... AWS_SECRET_ACCESS_KEY=...
# LTX25_MODAL_MEDIA_SECRET=s3-media
