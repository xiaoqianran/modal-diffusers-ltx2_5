fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic-settings>=2.7,<3
python-multipart>=0.0.20,<1
httpx>=0.28,<1
numpy>=1.26,<3
Pillow>=10,<12
# Torch is installed separately from the official PyTorch cu130 wheel index in
# Dockerfile so the CUDA build is explicit and reproducible on Blackwell.
kernels==0.17.1
transformers==5.17.0
diffusers @ git+https://github.com/huggingface/diffusers.git@80c7ed262aeffbeb43ef13ae04baeb9b84515a69
# Keep the resident Qwen stack bit-for-bit aligned with the dual-resident
# RTX PRO 6000 probe that completed Qwen load-after-LTX in 8.667s.
accelerate==1.15.0
safetensors==0.8.0
peft==0.21.0
huggingface-hub==1.32.0
bitsandbytes==0.50.2
imageio-ffmpeg>=0.6
imageio>=2.35,<3
av>=14,<17
boto3>=1.40,<2
